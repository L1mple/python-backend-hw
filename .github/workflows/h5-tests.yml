name: "HW5 Tests"

on:
  pull_request:
    branches: [ main ]
    paths: [ 'hw5/hw/**' ]
  push:
    branches: [ main ]
    paths: [ 'hw5/hw/**' ]

jobs:
  test-hw5:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # 🔎 Étape de diagnostic : afficher l’arborescence complète
    - name: Afficher le répertoire courant et son contenu
      run: |
        echo "Répertoire racine du dépôt :"
        pwd
        ls -R hw5 || true

    # 🧭 Définir le PYTHONPATH sur le bon dossier
    - name: Définir le PYTHONPATH
      run: |
        echo "Définition du PYTHONPATH..."
        echo "PYTHONPATH=${{ github.workspace }}/hw5/hw" >> $GITHUB_ENV
        echo "PYTHONPATH défini sur : ${{ github.workspace }}/hw5/hw"

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      working-directory: hw5/hw
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-test.txt

    # ⚙️ Installation du package local (facultatif, mais utile si setup.py/pyproject.toml existe)
    - name: Install local package
      working-directory: hw5/hw
      run: |
        pip install -e .

    # 🧪 Lancement des tests avec couverture
    - name: Run tests with coverage
      working-directory: hw5/hw
      env:
        DATABASE_URL: postgresql://postgres:password@postgres:5432/test_db
        PYTHONPATH: ${{ github.workspace }}/hw5/hw
      run: |
        echo "Chemins utilisés par Python :"
        python -c "import sys; print(sys.path)"
        pytest --cov=shop_api --cov-report=xml --cov-report=term-missing -v

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: hw5/hw/coverage.xml
        flags: unittests
        fail_ci_if_error: false

    - name: Check 95% coverage threshold
      working-directory: hw5/hw
      run: |
        python -m coverage report --fail-under=95
