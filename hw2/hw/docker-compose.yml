services:
  shop_api:
    build:
      context: shop_api
      dockerfile: Dockerfile
    ports: ["8000:8000"]
  shop_client:
    build:
      context: shop_client
      dockerfile: Dockerfile
    ports: ["8001:8001"]
    depends_on: [shop_api]
