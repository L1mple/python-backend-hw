# Postgres (используются и docker-compose, и приложение)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=shop
DB_USER=shop
DB_PASSWORD=shop

# Полная строка подключения; если задана — она имеет приоритет
# Примеры:
# DATABASE_URL=postgresql+psycopg2://shop:shop@localhost:5432/shop
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/postgres
DATABASE_URL=
