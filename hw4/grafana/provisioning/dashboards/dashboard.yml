apiVersion: 1

providers:
  - name: "Shop API Technical"
    folder: "Tech"
    options:
      path: /etc/grafana/provisioning/dashboards
  - name: "Shop API Business"
    folder: "Business"
    options:
      path: /etc/grafana/provisioning/dashboards
